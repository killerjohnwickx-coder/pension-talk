# 펜션톡 - 휴대폰 설치 가이드 v2 (마이크 활성화 + PWA)

**v2 보강 (2026-05-13)**: HTTPS 필수 안내·마이크 권한 가이드·로컬 서버 옵션·정식 호스팅 path

---

## ⚠ 마이크 사용 필수 조건 — HTTPS 환경

브라우저는 **HTTPS** 또는 **localhost** 환경에서만 마이크 접근을 허용합니다. `file://` 또는 일반 `http://`는 마이크 차단.

→ 휴대폰에서 마이크 음성인식을 사용하려면 **아래 옵션 중 1개** 선택 필수.

---

## 📱 옵션 A: GitHub Pages 무료 호스팅 (권장·최단·HTTPS 자동)

1회 설정·영구 무료·HTTPS 자동·휴대폰 어디서나 접속.

### 1단계 (PC, 5분)
1. https://github.com 계정 로그인 (없으면 가입)
2. **New repository** → 이름 `pension-talk` (또는 자유)·**Public** 체크
3. 4 파일 업로드: `index.html`·`manifest.json`·`sw.js`·`icon.svg`
4. Repository **Settings** → **Pages** → Source: `main` branch → **Save**
5. 1~2분 후 URL 생성: `https://<github아이디>.github.io/pension-talk/`

### 2단계 (휴대폰, 1분)
1. Chrome (Android) 또는 Safari (iOS)로 위 URL 접속
2. 마이크 버튼 첫 사용 시 **권한 허용**
3. 홈 화면 추가 → 앱처럼 사용

---

## 🏠 옵션 B: 로컬 PC HTTPS 서버 (Wi-Fi 같은 망 필요)

사용자 PC + 휴대폰이 같은 Wi-Fi 망에 있을 때.

### 1단계 (PC, 명령 프롬프트)
```
cd C:\Users\user\Desktop\자비스공유\통역앱_숙박소상공인
python -m http.server 8000
```
→ `http://0.0.0.0:8000` 가동.

### 2단계 (HTTPS 인증서 발급, 1회)
```
pip install pyOpenSSL
python -c "from OpenSSL import crypto; ..."  (또는 mkcert 사용)
```
→ 자비스가 추가 가이드 제공 가능 (사용자 요청 시).

### 3단계 (휴대폰)
1. 같은 Wi-Fi 연결
2. 브라우저로 `https://<PC_IP>:8443` 접속 (PC IP는 `ipconfig` 확인)
3. 인증서 경고 무시 (자체서명) → 허용

→ PC 켜져 있을 때만 작동.

---

## 🔌 옵션 C: ngrok 터널 (5분·임시 무료 HTTPS)

```
ngrok http 8000
```
→ 임시 HTTPS URL 발급. 단, 무료 plan은 8시간 후 URL 변경됨.

---

## 📲 옵션 D: 카톡 전송 + 휴대폰 브라우저 (마이크 제약)

PC에서 HTML 4 파일을 zip → 카톡 전송 → 휴대폰에서 풀고 Chrome으로 `file://` 열기.
- ✅ PWA 설치 가능
- ❌ **마이크 사용 불가** (file:// 환경)

→ 음성 안 쓰고 텍스트·템플릿만 사용 시 유효.

---

## 🎤 마이크 첫 사용 시

1. 마이크 버튼 (🎤) 터치
2. 브라우저 권한 팝업 → **"허용"** 터치
3. 한국어로 말하기 → 자동 변환
4. 외국어 응답 → 🔊 버튼으로 음성 재생

### 권한 거부 시 복구
- Chrome: 주소창 자물쇠 아이콘 → 마이크 → "허용"
- Safari: 설정 → Safari → 카메라/마이크 → "허용"

---

## 📦 4 파일 구성

| 파일 | 용도 |
|---|---|
| `index.html` | 메인 앱 (음성·번역·UI) |
| `manifest.json` | PWA 설정 (앱 이름·아이콘) |
| `sw.js` | Service Worker (오프라인 캐시) |
| `icon.svg` | 앱 아이콘 |

---

## 🔧 권장 path (자비스 권고)

| 사용자 상황 | 옵션 |
|---|---|
| 5분 내 휴대폰 사용·영구 | **A. GitHub Pages** |
| PC 켜져 있을 때만·테스트 | B. 로컬 HTTPS |
| 즉시 임시 테스트 | C. ngrok |
| 텍스트만·마이크 X | D. 카톡 file:// |

→ **사용자 권고: A (GitHub Pages)**. 1회 설정 후 영구·무료·HTTPS·휴대폰 어디서나.

---

## 📞 자비스 자율 추가 지원 가능

- GitHub Pages 설정 단계별 가이드 (스크린샷 포함 docx 작성 가능)
- 로컬 HTTPS 서버 자동 가동 스크립트 (`.bat` 파일)
- ngrok 자동 설정 가이드
- 마이크 권한 트러블슈팅

사용자 요청 시 즉시 작성.

---

## 📅 Q3 이후 (사업화 결정 시)

정식 사업화 시 GitHub Pages → 자체 도메인 (예: `pensiontalk.kr`) + 정식 호스팅. Q3 결정 후 자비스 자율 마이그레이션.

---

제이크의 자비스 제작 | v2 2026-05-13 보강
